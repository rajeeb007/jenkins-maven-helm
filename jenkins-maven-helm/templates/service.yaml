apiVersion: v1
kind: Service
metadata:
  name: {{ include "jenkins-maven-helm.fullname" . }}
  labels:
    {{- include "jenkins-maven-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jenkins-maven-helm.selectorLabels" . | nindent 4 }}
